# 第八步：ResearchEvidence、record review 与 output lineage 实施计划

## Task 1：TDD 与 schema

- [x] 1.1 evidence identity/项目 relation/验证状态回归先失败。
- [x] 1.2 record version/review audit/CAS 回归先失败。
- [x] 1.3 output immutable version/parent/main-only manifest 回归先失败。

## Task 2：领域服务与兼容投影

- [x] 2.1 实现 ResearchRepository evidence upsert/verify/relation cursor。
- [x] 2.2 现有 AI record 更新双写版本与审计；confirmed 必须有 verified provenance。
- [x] 2.3 research/AI output 保存 immutable version 与 input manifest；旧 output 继续读取。

## Task 3：门禁与文档

- [x] 3.1 更新模块 07/10/11/14、台账和脚本说明。
- [x] 3.2 运行专项、完整 check/build/smoke/diff/open-source 门禁。
- [x] 3.3 保留本地等待最终用户验收，不 commit/push/PR/tag/Release。

## 验证结果

- `npm run check`：通过，包含 typecheck、280 个 preload API/271 个 main handler 契约、研究谱系、OCR、检索、阅读器、备份和开源卫生门禁。
- `npm run build`：通过；main、preload、renderer 生产构建成功。
- `npm run smoke`：通过；Electron 冒烟测试成功。
- `git diff --check`：通过，仅有仓库既有 CRLF 转换提示；`package-lock.json` 无变化，无残留 Electron 进程。

## 边界

- evidence 本体唯一，项目 note/tags/status 放 relation；不复制 canonical excerpt 形成多个身份。
- 本步实现最小 input manifest/coverage；完整 claim manifest 的自动句段绑定继续后续切片。
- 旧 research_notes、ai_research_records、research_outputs 保留兼容，不做破坏性回填。
