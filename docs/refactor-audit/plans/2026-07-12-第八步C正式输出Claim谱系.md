# 第八步 C：正式输出 claim manifest 实施计划

## Task 1：RED

- [x] 1.1 句段枚举覆盖重复文本、UTF-16/非 BMP、CRLF/LF 与空内容。
- [x] 1.2 formal coverage、未知 provenance、错误范围和 stale source 先失败。
- [x] 1.3 SQLite manifest/entry 不可变、output 关联和旧输出兼容先失败。

## Task 2：GREEN

- [x] 2.1 实现确定性 claim 枚举与 main-only manifest builder。
- [x] 2.2 additive 保存 manifest/entries；formal 必须全部 supported 且 provenance 属于 input manifest。
- [x] 2.3 现有无逐句来源 AI 报告降为 draft；不伪造自动绑定。
- [x] 2.4 提供有界读取/验证 IPC，不允许 renderer 改写历史 manifest。

## Task 3：REFACTOR 与门禁

- [x] 3.1 更新模块 07/10/11/14、台账和脚本说明。
- [x] 3.2 运行专项、完整 check/build/smoke/diff/open-source。
- [x] 3.3 仅保留本地等待用户验收，不 commit/push/PR/tag/Release。

## 验证结果

- RED：`npm run check:research-claim-manifest` 因实现文件不存在按预期失败；GREEN 后纯函数与 SQLite lineage 回归通过。
- `npm run check`：通过，包含 286 个 preload API、277 个 main handler、claim/aggregate/lineage、OCR、检索、阅读器、备份和开源门禁。
- `npm run build`、`npm run smoke`：通过。
- `git diff --check`：通过，仅有仓库既有 CRLF 提示；`package-lock.json` 无变化，无残留 Electron 进程。

## 边界

- 第一版把非空句段保守视为待支持 claim；不使用模型猜“事实句”。
- evidence/aggregate ID 必须已存在于该 output 的 main-built input manifest，并在 formal 写入时重新验证。
- 自动生成但没有逐句映射的输出是 draft；输入里有证据不等于每句话已获支持。
- 不新增 tokenizer、NLP 模型或第三方依赖。
