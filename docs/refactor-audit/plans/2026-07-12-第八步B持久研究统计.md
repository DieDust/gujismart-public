# 第八步 B：持久 ResearchAggregateArtifact 实施计划

## Task 1：可信来源与 schema

- [x] 1.1 搜索响应在 main 内登记 snapshot aggregate summary，renderer 不提交结果 payload。
- [x] 1.2 additive 新增 immutable aggregate artifact 与 project relation。
- [x] 1.3 覆盖 active/expired/stale、幂等、generation 变化、cursor 和重启回归。

## Task 2：领域接口与兼容

- [x] 2.1 实现 promote/validate/list relation 领域服务。
- [x] 2.2 接入 search IPC/preload/function contract，所有列表有界。
- [x] 2.3 更新模块 06/07/11/14 与台账。

## Task 3：门禁

- [x] 3.1 专项 typecheck/aggregate/search 回归。
- [x] 3.2 完整 check/build/smoke/diff/open-source。
- [x] 3.3 仅保留本地，等待用户最终验收；不 commit/push/PR/tag/Release。

## 验证结果

- `npm run check`：通过；包含 283 个 preload API、274 个 main handler、aggregate/lineage、OCR、检索、阅读器、备份和开源门禁。
- `npm run build`、`npm run smoke`：通过。
- `git diff --check`：通过，仅保留仓库既有 CRLF 提示；`package-lock.json` 无变化。

## 边界

- SearchSnapshot 仍是 TTL 执行状态；只有 main 已登记的统计摘要可以晋升。
- 第一版冻结 totals/status/warnings/exactness/coverage，不把 preview 伪装成 exact。
- 本步不实现逐句 claim manifest，也不引入 tokenizer、模型或第三方依赖。
