# 第九步 C：导出快照与原子发布

## Task 1：RED

- [x] 1.1 目标文件已存在时，渲染或校验失败不得破坏旧文件。
- [x] 1.2 并发导出必须使用同目录随机暂存文件，失败后不得残留暂存文件。
- [x] 1.3 导出快照、产物哈希、大小、stale/corrupt 和 cursor 回归先失败。

## Task 2：GREEN

- [x] 2.1 建立 `AtomicExportWriter`，统一暂存、校验、发布、回滚和清理。
- [x] 2.2 建立只增不改的 `ExportSnapshot` 与 `ExportArtifact`，冻结正文来源和导出参数。
- [x] 2.3 所有文本、HTML、XML、JSON、PDF 导出统一写到暂存路径，通过校验后发布。
- [x] 2.4 PDF 的 HTML 中间文件改用随机 ID，消除同毫秒碰撞。

## Task 3：门禁

- [x] 3.1 纳入导出回归、类型检查和最终完整 check/build/smoke。
- [x] 3.2 更新模块 10/12/14、总台账、脚本说明和总 TODO。

## 边界

- 不增加依赖、模型或外部服务。
- 数据库只增加表和索引，旧导出 API 返回值保持兼容。
- 快照记录文献与页级 canonical/version 引用，不复制正文大对象。
- 正式发布、GitHub Actions、标签与 Release 继续等待本地验收。
