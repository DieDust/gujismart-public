# 第六步：StableReaderLocator v2 实施计划

## Task 1：TDD 与 shared 合同

- [x] 1.1 exact/block/page/document 判别联合与运行时校验先失败。
- [x] 1.2 legacy 搜索定位诚实降级和 exact 升级矩阵先失败。
- [x] 1.3 quote/prefix/suffix 重定位与 unresolved 回归先失败。

## Task 2：兼容适配

- [x] 2.1 实现唯一 shared locator 模块，不新增第二套领域类型。
- [x] 2.2 SearchHit 双写 stable locator；旧 SearchHitLocator 保留兼容。
- [x] 2.3 工作区、研究摘录和阅读入口兼容解析 v2，page/document 不伪造字符范围。

## Task 3：门禁与文档

- [x] 3.1 更新模块 05/06/07/10/14、台账和脚本说明。
- [x] 3.2 运行 locator 专项、完整 check/build/smoke/diff/open-source 门禁。
- [x] 3.3 保留本地等待最终用户验收，不 commit/push/PR/tag/Release。

## 验证记录

- `npm run check`、`npm run build`、`npm run smoke`：通过。
- `npm run test:search`、workspace/search return/research 定向回归：通过。
- `git diff --check`：通过，仅有既有 CRLF 提示；`package-lock.json` 未修改。
- 未执行 commit、push、PR、Actions、tag 或 Release。

## 边界

- 不新增依赖，不破坏旧数据库，不批量重写 `locator_json` 或 reader state。
- 本步建立定位身份、验证和兼容适配；SearchSnapshot/EvidenceResolver 属于第七步。
- 旧定位缺 source hash/content version 时最多降为 block/page/document，绝不伪装 exact verified。
