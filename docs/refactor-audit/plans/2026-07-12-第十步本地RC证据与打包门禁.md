# 第十步：本地 RC 证据与打包门禁

## Task 1：合成验收数据

- [x] 1.1 固定 seed 的古籍扫描/OCR/竖排样本，不含真实文库内容。
- [x] 1.2 固定 seed 的现代论文/表格/引用样本，不含受限论文正文。
- [x] 1.3 fixture manifest 声明来源、用途、许可和禁止替换为真实用户数据。

## Task 2：供应链与包内材料

- [x] 2.1 从 lockfile 生成 SPDX SBOM，缺失许可证必须显式标记。
- [x] 2.2 对 bundled vendor 逐文件生成 SHA-256 manifest。
- [x] 2.3 Setup/Portable/unpacked 包必须携带 LICENSE、NOTICE、THIRD_PARTY_NOTICES、SBOM 和 vendor manifest。

## Task 3：本地候选验收

- [x] 3.1 生成包含 commit、dirty 状态、lock hash、构建产物 hash 和门禁结果的本地 RC manifest。
- [x] 3.2 `win-unpacked` 使用隔离用户目录完成 packaged smoke。
- [x] 3.3 完整 check/build/smoke/opensource/mojibake/diff 通过后交给用户本地验收。

## 发布冻结

- 本步骤不 push、不触发 Actions、不创建 tag/PR/Release。
- 用户明确确认本地候选无问题并批准公开前，RC manifest 只能标记 `local-test-pending`。
