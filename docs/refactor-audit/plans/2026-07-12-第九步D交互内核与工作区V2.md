# 第九步 D：交互内核与工作区 V2

## Task 1：RED

- [x] 1.1 CloseCoordinator 必须等待 dirty 参与者并汇总 needs-confirmation/failed。
- [x] 1.2 latest-request-wins、拖拽 preview/commit/cancel 和重复提交回归先失败。
- [x] 1.3 工作区 v2、v1 兼容读取、last-known-good 恢复和容量清洗回归先失败。

## Task 2：GREEN

- [x] 2.1 增加零依赖交互内核，供设置、阅读器、检索和拖拽逐步接入。
- [x] 2.2 Esc 只处理拖拽、浮层、瞬时模式和返回，不再隐式关闭应用。
- [x] 2.3 工作区使用 v2 主快照与 last-known-good，继续兼容 v1。
- [x] 2.4 标签拖拽只在 pointer-up 提交一次，取消不修改真实顺序。

## Task 3：门禁

- [x] 3.1 更新 workspace/tab/interaction 回归并纳入最终完整门禁。
- [x] 3.2 更新模块 09/12/14 和总 TODO。

## 边界

- 第一阶段不新增数据库表或运行时依赖。
- 工作区只保存轻量 descriptor，不保存正文、密钥或完整检索结果。
- 显式退出仍由用户命令触发；任何 dirty 放弃必须明确确认。
